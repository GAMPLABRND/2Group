# 결정 기록

| 번호 | 결정 | 근거 | 영향 파일 | 날짜 |
|---|---|---|---|---|
| DEC-001 | DOCX 변환본의 7.1.1부터 7.1.10까지에 임시 조항 ID `URS-F-001`부터 `URS-F-010`을 순서대로 부여함 | Word 표의 URS 번호 열이 비어 있고 자동 번호는 소절 제목에만 남아 있어 하네스 인벤토리가 기능 조항을 탐지하지 못함. 원본 DOCX는 변경하지 않음 | `docs/urs/02 KPBMA-EDU-00X-URS 1조 전자로그북 관리 시스템 사용자 요구사항 규격서.md`, `harness/state/URS_STATUS.json` | 2026-08-25 |
| DEC-002 | 시스템 제목을 `CSV실습과정 2조 전자로그북`으로 확정함 | URS 표지와 본문은 2조이며 파일명의 1조와 불일치함 | `lib/brand.ts`, `app/layout.tsx` | 2026-08-25 |
| DEC-003 | 시드 계정 표시명과 사번을 관리자 EDU2-001, 사용자 EDU2-002, 검토자 EDU2-003으로 정함 | URS 근거 없음, 기본값. URS-F-008의 사번 필수와 일관 표시를 충족함 | `lib/schema.ts`, `app/api/seed/route.ts` | 2026-08-25 |
| DEC-004 | 비밀번호 정책 기본값을 최소 4자, 조합 미사용, 유효기간 90일, 5회 실패 잠금, 30분 미활동 로그아웃으로 정함 | URS 근거 없음, 기본값. 초기 비밀번호 1234와 호환함 | `lib/schema.ts`, 관리자 설정과 로그인 API | 2026-08-25 |
| DEC-005 | 세 SPEC의 필드명 차이는 기존 USERS 헤더를 보존하고 `lib/schema.ts`의 헤더를 통합 정본으로 사용함 | 기존 시트 열 이름 변경과 삭제 금지, D1부터 D4의 교차 의존성 해소 | `lib/schema.ts`, `PLAN.md` | 2026-08-25 |
| DEC-006 | 동일 장비 동시 시작은 프로세스 내 장비 잠금, 점유 후보 기록 ID 기록 후 재조회, 추가 후 중복 검증과 무효 보상으로 차단함 | URS-F-002. 고정 Sheets 규격에 분산 조건부 원자 갱신이 없으므로 다중 인스턴스 검증은 외부 환경 증거가 필요함 | `app/api/records/_lib/service.ts`, `app/api/records/start/route.ts` | 2026-08-25 |
| DEC-007 | 로그북 문서번호를 `ELMS-LB-[장비 코드]-[조회 시작일 YYYYMMDD]-[조회 종료일 YYYYMMDD]-[출력 일시 YYYYMMDDHHMMSS]` 형식으로 생성함 | URS 근거 없음, 기본값. URS-F-007의 문서 식별, 조회 조건과 출력 시각을 함께 표시함 | `app/api/print/_shared.ts` | 2026-08-25 |
| DEC-008 | 개발 서버에 사설 IPv4 패턴 `10.*`, `172.*`, `192.168.*`와 현재 Wi-Fi 호스트 `192.168.9.6`을 개발 출처로 등록함 | URS 개정 대상. 교육장 LAN의 여러 PC에서 접속할 때 Next.js 개발 리소스 차단을 방지함. Next.js 16이 IP 와일드카드를 현재 호스트와 일치시키지 않아 실제 호스트를 함께 등록함 | `next.config.ts` | 2026-08-26 |
| DEC-009 | 장비의 교정 대상 여부와 적격성평가 대상 여부를 독립 체크박스와 독립 저장값으로 분리함 | URS 개정 대상. URS-F-001 원문은 두 항목을 결합하지만 사용자가 URS-F-006 검토와 URS-F-007 조회 및 출력에서 각각 확인하도록 요청함. 기존 행은 새 값 저장 전까지 기존 결합값을 적격성평가 초기값으로 해석함 | `app/equipment/EquipmentConsole.tsx`, `app/api/equipment/route.ts`, `lib/schema.ts`, `types.ts` | 2026-08-26 |
| DEC-010 | 교정 대상 장비의 유효기간이 KST 현재 날짜보다 과거가 되면 사용 상태를 `사용중지`로 자동 전환함 | URS 개정 대상. URS-F-002 원문은 사용 시작 제한만 규정하지만 사용자가 만료 시 사용가능 표시도 즉시 사용중지로 바뀌도록 정정 요청함. 전환은 SYSTEM 행위자와 자동 전환 사유로 감사추적함 | `lib/equipment.ts`, `app/api/equipment/route.ts`, `app/api/records/_lib/service.ts`, `app/api/dashboard/data.ts`, `app/api/alarms/data.ts` | 2026-08-26 |
| DEC-011 | About의 실습자 정보를 `TRAINING_PROFILE.members_json`의 가변 배열로 관리하고 기존 회사명과 실습자명 열은 첫 번째 실습자 값으로 유지함 | URS 개정 대상. 기존 열 변경과 삭제 금지 규칙을 지키면서 인원 증감, 최소 1명 검증, 전체 목록 감사추적을 지원함. 기존 행에 새 값이 없으면 한국제약바이오협회 소속 실습자 6명을 기본 표시함 | `app/about/**`, `app/api/admin/**`, `app/api/seed/route.ts`, `lib/schema.ts`, `lib/training-profile.ts` | 2026-08-26 |
| DEC-012 | 자동 백업 기본값을 1일 주기 13:00 KST로 바꾸고 매분 보호 스케줄러가 설정을 판정하여 운영 시트 전체를 보호된 Google Drive 폴더의 새 XLSX 파일로 보관함 | URS 개정 대상. URS-D-003 원문은 01:00을 규정하지만 사용자가 13:00과 ADMIN 가변 설정을 요청함. Vercel Cron은 동적 설정을 직접 바꿀 수 없으므로 매분 서버 호출과 저장 설정 판정을 분리함. 파일 수정과 삭제 API를 두지 않고 생성 SHA-256을 다운로드 때 검증함 | `vercel.json`, `app/backup/**`, `app/api/backup/**`, `app/api/cron/backup/**`, `lib/backup.ts`, `lib/schema.ts` | 2026-08-26 |
| DEC-013 | 미확인 알람은 계산된 알람의 안정 키와 사용자 ID 조합을 `ALARM_ACKS`에 추가하여 읽음 상태를 보존하고 로그인 레이아웃에서 한 번만 조회함 | URS 개정 대상. 사용자가 팝업에서 읽음 처리 또는 상세 이동을 요청했으며 업무 데이터를 브라우저 저장소에 둘 수 없고 폴링을 만들 수 없음. ADMIN 전용 백업과 보안 알람은 일반 역할의 알람 목록 생성 단계에서 제외함 | `components/UnreadAlarmPopup.tsx`, `app/api/alarms/unread/route.ts`, `lib/alarm-notifications.ts`, `lib/schema.ts` | 2026-08-26 |
| DEC-014 | 교정 대상 또는 적격성평가 대상 중 하나라도 선택한 장비는 교정 유효기간을 필수로 입력함 | URS 개정 대상. 두 대상 여부는 독립 값을 유지하되 사용자의 추가 요구에 따라 적격성평가 대상만 선택한 경우에도 날짜를 보존하고 빈 값 저장을 서버에서 차단함. 기존 적격성평가 전용 시드 장비에는 2027-06-30을 교육 기본값으로 지정함 | `app/equipment/EquipmentConsole.tsx`, `app/api/equipment/route.ts`, `lib/schema.ts` | 2026-08-26 |
| DEC-015 | 백업 XLSX를 시스템 또는 Google Drive 경로에 보관하지 않고 ADMIN 요청의 동일 응답으로 브라우저에 전달하여 사용자 PC에 수동 저장함 | URS 개정 대상. 사용자의 최신 요청이 DEC-012의 보호 Drive 저장과 자동 스케줄 방식을 대체함. 브라우저가 없는 자동 작업은 사용자 PC에 파일을 저장할 수 없으므로 Cron을 중단하고, 과거 이력 재다운로드 대신 새 백업 생성만 허용함. 기존 시트 열은 삭제하지 않고 Drive ID를 빈 값으로 유지함 | `vercel.json`, `.env.example`, `app/backup/**`, `app/api/backup/**`, `app/api/cron/backup/**`, `app/alarms/page.tsx`, `lib/backup.ts`, `README.md`, `SPEC_C.md`, `PLAN.md` | 2026-08-26 |
